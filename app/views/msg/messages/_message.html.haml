- message ||= @message

.message{:id => "message_#{message.id}", :data => {:refreshable => true, :url => message_path(message, :format => :js)} }
  %h2.name
    = link_to shorten(message.subject, 54), msg.message_url(message), :class => "message"
    = action_menulink(message)
  = action_menu(message)
  %p.description
    = message.description
  %p.excerpt
    = shorten(message.body, 128)